version: '3'

services:
  web: &web
    tty: true
    stdin_open: true
    build: .
    shm_size: '256mb'
    command: 'ruby server.rb'
    volumes:
      - .:/app
    ports:
      - 9494:9494
  test:
    <<: *web
    command: 'bundle exec rspec'